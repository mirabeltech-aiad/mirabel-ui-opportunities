
import React from "react";

const ProposalTimelineSection = ({ proposalId }) => {
  return (
    <div className="text-center py-8 text-gray-500">
      <p>Timeline functionality for Proposal #{proposalId} will be implemented here.</p>
    </div>
  );
};

export default ProposalTimelineSection;
