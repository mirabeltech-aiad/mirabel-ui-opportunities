
import React from "react";

const ProposalStageTrailSection = ({ proposalId }) => {
  return (
    <div className="text-center py-8 text-gray-500">
      <p>Stage progression for Proposal #{proposalId} will be implemented here.</p>
    </div>
  );
};

export default ProposalStageTrailSection;
