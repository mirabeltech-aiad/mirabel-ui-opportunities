
import React from "react";

const ProposalStatsSection = ({ proposalId }) => {
  return (
    <div className="text-center py-8 text-gray-500">
      <p>Analytics for Proposal #{proposalId} will be implemented here.</p>
    </div>
  );
};

export default ProposalStatsSection;
