
import React from "react";

const ProposalTasksSection = ({ proposalId }) => {
  return (
    <div className="text-center py-8 text-gray-500">
      <p>Tasks functionality for Proposal #{proposalId} will be implemented here.</p>
    </div>
  );
};

export default ProposalTasksSection;
